server {
  server_name assets.*;
  listen  80;
  # OPTIONAL SSL
  # listen  443 ssl;

  # ssl_certificate       /etc/nginx/ssl/fullchain.pem;
  # ssl_certificate_key   /etc/nginx/ssl/privkey.pem;
  # END OPTIONAL SSL

  location / {
    root /var/www/assets;
  }


}
